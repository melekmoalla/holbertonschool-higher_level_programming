#!/usr/bin/python3

"""Module to find the max integer in a list
"""


def lazy_matrix_mul(m_a, m_b):
    import numpy as np

    """Module to find the max integer in a list
    """

    return (np.dot(m_a, m_b))
